<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xyclub.practice.server.dao.SubjectCategoryDao">

    <!-- 查询包含题目的所有一级分类（按父分类ID分组统计题目数） -->
    <select id="getPrimaryCategory" resultType="com.xyclub.practice.server.entity.po.PrimaryCategoryPO">
        select
        count(distinct a.subject_id) as subjectCount,
        b.parent_id as parentId
        from subject_mapping a,
        subject_category b,
        subject_info c
        where a.category_id = b.id
        and a.subject_id = c.id
        and a.is_deleted = 0
        and b.is_deleted = 0
        and c.is_deleted = 0
        and c.subject_type in
        <foreach collection="subjectTypeList" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
        group by b.parent_id
    </select>

    <!-- 根据主键查询分类 -->
    <select id="selectById" resultType="com.xyclub.practice.server.entity.po.CategoryPO">
        select id,
               category_name as categoryName,
               category_type as categoryType,
               parent_id     as parentId
        from subject_category
        where is_deleted = 0
          and id = #{id}
    </select>

    <!-- 按父分类ID与分类类型查询二级分类列表 -->
    <select id="selectList" resultType="com.xyclub.practice.server.entity.po.CategoryPO">
        select id,
               category_name as categoryName,
               category_type as categoryType,
               parent_id     as parentId
        from subject_category
        where is_deleted = 0
          and parent_id = #{parentId}
          and category_type = #{categoryType}
    </select>

</mapper>
