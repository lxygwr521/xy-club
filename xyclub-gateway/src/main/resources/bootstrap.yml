spring:
  application:
    name: xyclub-gateway-dev
  profiles:
    active: dev
#      Nacos 注册发现配置
  cloud:
    nacos:
      config:
        server-addr: 182.92.176.188:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 182.92.176.188:8848
